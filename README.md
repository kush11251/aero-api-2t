# Aero API
A high-performance aviation weather forecasting system.
## Installation
1. Initialize the Go module: `go mod init aero-api`
2. Get the dependencies: `go get -u github.com/gorilla/mux`
3. Run the application: `go run main.go`